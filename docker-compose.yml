version: "3.9"

services:
  app:
    build:
      context: .
      args:
        - DEV=true
    image: blood_net_app
    container_name: app
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"

    networks:
      - blood_net_network

networks:
  blood_net_network:
    driver: bridge